name: ultimate_gpt
description: A new Auto-GPT project.
publish_to: 'none'
version: 1.0.1
environment:
  sdk: '>=2.19.6 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  ftpconnect: ^2.0.2
  http: ^0.13.3
  openai: ^1.0.0
  tinycolor: ^1.0.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true